# מסמך חוקיות – התגלגלות בקשת טיסה (אולר ↔ חטיבה)

מסמך זה מגדיר את החוקיות המבצעית סביב בקשת טיסה: מי רשאי לפעול עליה בכל שלב, איך פעולה של גורם אחד משתקפת אצל הגורם השני, ומה קורה בפועל במימוש הקיים כיום לעומת מה שצריך לקרות. הוא נכתב כתשובה ישירה לארבע השאלות שהועלו, ובנוסף פורש את מלוא מסלול חייה של בקשה.

כל סעיף מבחין בין **מצב קיים (כפי שהמערכת מתנהגת היום)** לבין **חוקיות מומלצת**, במקומות שבהם יש פער. פערים קריטיים מסומנים ב-⚠️.

---

## 1. הגורמים במערכת

| גורם | אפליקציה | תפקיד |
|---|---|---|
| **מפעיל** | אולר (Alar) | יוצר בקשת טיסה, משייך רחפן/ים, משרטט/בוחר פוליגון, עוקב אחרי הסטטוס, מפעיל/מסיים טיסה בפועל |
| **קצין רוק״ק / חמ״ק** | חטיבה (Web / HQ) | בוחן בקשות ממתינות, מאשר/דוחה/מסמן קונפליקט, יכול לערוך גבולות פוליגון בזמן אישור, מנהל תמונת מצב גזרתית |
| **מאגר מרכזי (Relay)** | `backend-relay` | מתווך בין שתי האפליקציות: שומר את הבקשות בקובץ, ומשדר (broadcast) כל הודעה שמתקבלת לכל שאר הלקוחות המחוברים |

### ⚠️ הערה קריטית – אין הרשאות משתמש היום
נכון להיום **אין מנגנון התחברות/זיהוי משתמש** באף אחת מהאפליקציות. "שם מפעיל" הוא שדה טקסט חופשי בפרופיל האולר, ול-HQ אין מסך התחברות בכלל. המשמעות:

- כל מי שיש לו גישה פיזית/רשתית למכשיר האולר יכול לפעול כאילו הוא המפעיל הרשום.
- כל מי שיש לו גישה לדפדפן ה-HQ יכול לאשר/לדחות/לערוך **כל** בקשה, ללא הבחנה בין "קצין רוק״ק" לכל משתמש אחר.

כלומר, ההרשאות המתוארות במסמך זה הן **ברמת סטטוס הבקשה בלבד**, לא ברמת זהות המשתמש. אם נדרשת אכיפה אמיתית ("רק X יכול לאשר", "רק היוצר יכול לערוך") — יש לבנות שכבת התחברות/הרשאות, שלא קיימת כיום. זו החלטת מוצר שממתינה להכרעה.

---

## 2. מודל הסטטוסים (תזכורת)

`PENDING_REVIEW` (ממתין לאישור) → `APPROVED` (מאושר) → `ACTIVE` (פעיל) → `COMPLETED` (הסתיים)
עם ענפי יציאה אפשריים בכל שלב: `REJECTED` (נדחה), `EXPIRED` (פג תוקף), `CONFLICT` (קונפליקט, ענף ביניים של PENDING_REVIEW).

`REJECTED`, `EXPIRED`, `COMPLETED` הם **מצבי סוף (Terminal)** — לא ניתן לערוך או לאשר בקשה שנמצאת בהם דרך שום מסך קיים.

---

## 3. שאלה 1 — מי יכול לערוך בקשה?

**מצב קיים:**

| סטטוס הבקשה | עריכה מהאולר | עריכה מה-HQ |
|---|---|---|
| `PENDING_REVIEW` | ✅ כפתור "ערוך" זמין — עריכה מלאה של כל השדות | ❌ אין מסך עריכה ל-HQ בשלב הזה (רק אישור/דחייה/קונפליקט) |
| `CONFLICT` | ❌ אין כפתור עריכה (רק ב-HQ ניתן להגיב) | ⚠️ ראה שאלה 3 — HQ יכול רק לשנות סטטוס/הערות, לא לערוך שדות |
| `APPROVED` | ✅ כפתור "ערוך" זמין — עריכה מלאה, **מחזירה את הבקשה ל-`PENDING_REVIEW`** | ✅ עריכת גבולות פוליגון בלבד ("ערוך גבולות") — לא משנה סטטוס |
| `ACTIVE` | ✅ כפתור "ערוך" זמין — עריכה מלאה, **מחזירה את הבקשה ל-`PENDING_REVIEW` גם אם הרחפן באוויר** ⚠️ | ❌ אין עריכה |
| `REJECTED` / `EXPIRED` / `COMPLETED` | ❌ אין כפתור עריכה בשום מסך | ❌ אין עריכה |

**⚠️ פער תפעולי לתשומת לב:** עריכת בקשה שנמצאת ב-`ACTIVE` (טיסה שכבר באוויר) מפילה אותה בחזרה ל-`PENDING_REVIEW` בדיוק כמו עריכת בקשה שעוד לא אושרה. זה נכון טכנית (המימוש לא מבחין בין "עריכת בקשה חדשה" ל"עריכת פעילות פעילה"), אבל מבצעית זה עלול ליצור מצב שבו רחפן ממשיך לטוס בעוד הבקשה שלו כבר לא מאושרת במערכת. יש להכריע:
- האם בכלל מותר לערוך בקשה שהיא `ACTIVE`?
- אם כן — אילו שדות מותר לערוך בלי "לאפס" את הסטטוס (למשל הערות בלבד), ואילו שדות (גובה, זמן, פוליגון) כן מחייבים אישור מחדש?

**חוקיות מומלצת:**
1. מפעיל יכול לערוך את **הבקשה שהוא יצר** כל עוד היא לא במצב סוף. עריכת שדות מהותיים (פוליגון, גבהים, זמן, רחפן) מחזירה לסטטוס `PENDING_REVIEW`. עריכת שדה לא-מהותי (הערות בלבד) לא צריכה לאפס סטטוס.
2. קצין רוק״ק יכול לערוך **גבולות פוליגון בלבד**, ורק על בקשה `APPROVED` — כחלק מתהליך האישור עצמו (Story 8 באפיון המקורי: "עריכת פוליגון במסגרת תהליך האישור"). זו לא "עריכת בקשה" חופשית, אלא צמצום/התאמת המרחב המאושר.
3. בקשה `ACTIVE` צריכה חוקיות ייעודית ונפרדת מ"עריכה רגילה" — ראו הפער למעלה.

---

## 4. שאלה 2 — מי רשאי לאשר בקשה?

**מצב קיים:** כל מי שנמצא במסך ה-HQ (web-app) יכול ללחוץ "אשר בקשה" / "סמן כקונפליקט" / "דחה", על **כל** בקשה שנמצאת ב-`PENDING_REVIEW` או `CONFLICT`. אין הבחנת תפקיד בין "צופה" ל"מאשר בפועל", ואין תיעוד אמיתי של זהות המאשר (אין שדה `approvedBy` שנשמר בנפרד מ"מי שלחץ על הכפתור באותו רגע").

**חוקיות מומלצת (מהאפיון המקורי, Story 5):**
- רק בעל הרשאה — קצין רוק״ק / חמ״ק — רשאי לאשר.
- בעת אישור נשמרים **זהות המאשר וזמן האישור** (`approvedBy`, `approvedAt`) — שדות שלא קיימים היום במודל הנתונים ויש להוסיף.
- דחיית בקשה מחייבת הזנת סיבה (קיים היום כ"הערות רוק״ק", אך לא נאכף כשדה חובה — ניתן לדחות בלי למלא כלום).

**מסקנה:** כדי לממש בפועל "מי ראשי לאשר" צריך (א) שכבת זיהוי משתמש/תפקיד ב-HQ, (ב) שמירת `approvedBy`/`approvedAt` בכל בקשה, (ג) אכיפת "הערות חובה" בדחייה.

---

## 5. שאלה 3 — עריכה על ידי משתמש בחטיבה (web) → איך זה משתקף באולר?

יש להבחין בין שני סוגי פעולה שה-HQ מבצע, כי הם מתנהגים **אחרת לגמרי**:

### 5א. שינוי סטטוס / הערות (אישור, דחייה, קונפליקט, הסרה)
✅ **עובד ומשתקף כראוי.** ה-HQ שולח הודעת `REVIEW_FLIGHT_REQUEST` דרך ה-Relay, שנשמרת בקובץ המרכזי ומשודרת (broadcast) לכל לקוח מחובר — כולל האולר. האולר מאזין להודעה הזו ומעדכן את הסטטוס וההערות בזמן אמת ברשימת הבקשות שלו.

⚠️ **אבל:** העדכון הוא **שקט** — אין התראה/צליל/הבהוב למפעיל שהבקשה שלו אושרה, נדחתה, או סומנה כקונפליקט. המפעיל יידע רק אם הוא נכנס ללשונית "בקשות טיסה" ובודק ידנית.

### 5ב. עריכת גבולות פוליגון על ידי HQ ("ערוך גבולות" בבקשה מאושרת)
⚠️⚠️ **לא עובד בכלל — זהו הפער המשמעותי ביותר שנמצא.** כאשר קצין רוק״ק עורך את גבולות הפוליגון של בקשה מאושרת דרך המפה, השינוי:
- **לא נשמר בשרת ה-Relay** (לא נשלחת שום הודעת WebSocket).
- **לא משודר לאולר** — המפעיל ממשיך לראות את הפוליגון המקורי שהוא ביקש, בעוד שבפועל המרחב שאושר לו שונה.
- **אובד ברענון דף** — כי הוא קיים רק ב-state המקומי של הדפדפן שביצע את העריכה.

זהו פער חמור ביחס לסיפור המקורי (Story 8 באפיון): "הגורם המבקש מקבל חיווי שהפוליגון שונה" ו"נשמרים הפוליגון המקורי והפוליגון שאושר" — אף אחד מהשניים לא קורה היום.

**נדרשת החלטה:** האם לתקן זאת (לשלוח `UPDATE_FLIGHT_REQUEST` בפועל בעת עריכת גבולות ב-HQ, ולהוסיף חיווי ברור למפעיל שהפוליגון שונה מהמבוקש) — זו לא שאלה של "האם", אלא איך ממש קריטית לכל תרחיש שבו HQ מצמצם/מזיז פוליגון לפני אישור.

---

## 6. שאלה 4 — האם מפעיל אולר יכול לערוך בקשה אחרי שהיא עלתה למפקדה?

**כן.** כל עוד הבקשה לא במצב סוף (`REJECTED`/`EXPIRED`/`COMPLETED`), המפעיל יכול לפתוח אותה מחדש ולערוך כל שדה — כולל בקשות שכבר `APPROVED` או אפילו `ACTIVE`.

**איך זה משתקף ב-HQ:** ✅ בכיוון הזה זה עובד נכון. עריכה מהאולר שולחת הודעת `UPDATE_FLIGHT_REQUEST` מלאה (כל שדות הבקשה) דרך ה-Relay, שנשמרת ומשודרת ל-HQ. הבקשה חוזרת ל-`PENDING_REVIEW` ומוצגת מחדש בתור הבקשות הממתינות אצל הקצין, "כאילו הוגשה מחדש" — כולל דגל ויזואלי בכרטיס העריכה שאומר "לאחר השמירה תחזור לסטטוס «ממתין לאישור»", כך שהמפעיל מודע לכך מראש.

**נקודה למחשבה:** בגלל שזה חל גם על `ACTIVE`, זה חוזר לפער שתואר בשאלה 1 — עריכת בקשה שהרחפן שלה כרגע באוויר "מבטלת" את אישורה במערכת, בלי שום אבחנה בין "ערכתי הערה" ל"שיניתי את הפוליגון תוך כדי טיסה".

---

## 7. טבלת סיכום — מי יכול לעשות מה, ואיך זה מסתנכרן

| פעולה | מבצע | תנאי סטטוס | משתקף בצד השני? | הערות |
|---|---|---|---|---|
| יצירת בקשה חדשה | מפעיל (אולר) | — | ✅ מיידי, `NEW_FLIGHT_REQUEST` | |
| עריכת בקשה (מלאה) | מפעיל (אולר) | לא-Terminal | ✅ מיידי, `UPDATE_FLIGHT_REQUEST`, מאפס ל-`PENDING_REVIEW` | גם עבור `ACTIVE` ⚠️ |
| אישור בקשה | קצין (HQ) | `PENDING_REVIEW` / `CONFLICT` | ✅ מיידי, `REVIEW_FLIGHT_REQUEST` | ללא חיווי/צליל למפעיל |
| דחיית בקשה | קצין (HQ) | `PENDING_REVIEW` / `CONFLICT` | ✅ מיידי | סיבה לא נאכפת כחובה |
| סימון קונפליקט | קצין (HQ) | `PENDING_REVIEW` | ✅ מיידי | |
| עריכת גבולות פוליגון | קצין (HQ) | `APPROVED` | ❌ **לא משודר בכלל** ⚠️⚠️ | לא נשמר גם בצד ה-HQ עצמו לאורך זמן |
| הסרת בקשה | קצין (HQ) | כל סטטוס לא-Terminal מתאים | ✅ מיידי, `REMOVE` | |
| פקיעת תוקף אוטומטית | המערכת (טיימר) | `APPROVED` בלבד | ✅ מיידי | תוקן בעבר כך שלא יחול על `PENDING_REVIEW` |
| התחלת/סיום פעילות | מפעיל (אולר) | `APPROVED` → `ACTIVE` → `COMPLETED` | תלוי מסלול קיים | |

---

## 8. פערים פתוחים הדורשים החלטה (סיכום)

1. **⚠️⚠️ עריכת גבולות פוליגון ב-HQ לא מסונכרנת בכלל** לאף מקום — לא לשרת, לא לאולר. הכי דחוף לתקן.
2. **⚠️ עריכת בקשה `ACTIVE` מהאולר מפילה אותה ל-`PENDING_REVIEW`** בלי אבחנה בין שינוי קריטי לשינוי קוסמטי.
3. **אין שכבת הרשאות/זיהוי משתמש** — "מי רשאי" הוא כרגע עניין של "מי יש לו גישה לאפליקציה", לא הרשאה אמיתית.
4. **אין שמירת `approvedBy`/`approvedAt`** בבקשה, ואין אכיפת סיבת דחייה כשדה חובה.
5. **אין חיווי אקטיבי (Push/צליל/הבהוב) למפעיל** כשסטטוס הבקשה שלו משתנה — הוא חייב לבדוק ידנית.

מומלץ להכריע איזה מהפערים הללו לתקן קודם — סעיף 1 הוא כנראה הכי דחוף כי הוא שובר את ההנחה הבסיסית ש"מה שמאושר ב-HQ הוא מה שהמפעיל רואה".
