# אפיון ממשק משתמש (UI/UX) וארכיטקטורה: מערכת WEB לתא רוק״ק חטיבתי
**מערכת שליטה ובקרה לניהול מרחב אווירי טקטי בזמן אמת**

---

## 1. מבוא ועקרונות UX מבצעיים
מערכת ה-WEB של תא הרוק"ק החטיבתי מהווה את הלב המבצעי לתיאום המרחב האווירי. מטרת עיצוב הממשק היא **הפחתת עומס קוגניטיבי** וסיוע בקבלת החלטות תחת לחץ זמנים קיצוני.

### עקרונות מובילים:
1. **עיצוב מבוסס "מידע דוחף" (Push-based):** המערכת מעדכנת את תמונת השמיים וההתרעות בזמן אמת ללא צורך ברענון מסך יזום (באמצעות WebSockets/gRPC Web).
2. **צבעוניות מבוססת סיווג צבאי סטנדרטי:**
   * **עמית / מורשה:** ירוק / כחול (ידידותי).
   * **חשוד / בלתי מזוהה:** צהוב / כתום.
   * **טורף / עוין:** אדום.
   * **חסימות ספקטרום / ל"א:** סגול.
3. **סביבת עבודה כהה (Dark Mode):** ממשק כהה כברירת מחדל, מותאם לעבודה ממושכת בחדרי מפקדה חשוכים (תאי שליטה) למניעת עייפות עיניים.

---

## 2. מודל הרשאות ותפקידים (Permissions Model)
המערכת מבוססת על שלושה פרופילי משתמשים ראשיים המנהלים את המרחב האווירי:

| תפקיד | הרשאות מפה ותצוגה | הרשאות ניהול בקשות טיסה | הרשאות אירועים מבצעיים |
| :--- | :--- | :--- | :--- |
| **קצין רוק"ק חטיבתי** *(Rokaq Officer)* | • צפייה מלאה בתמונת השמיים החטיבתית והאוגדתית.<br>• הגדרת פוליגונים ואזורי עניין. | • אישור / דחייה / החזרת בקשות להערות.<br>• עריכת נתיבים מוצעים.<br>• עקיפת חסימות אוטומטיות. | • פתיחה וסגירה של אירועים.<br>• הכרזת נוהל "נמר".<br>• הפעלת "פטיש אוויר".<br>• הפצת התרעות לשטח. |
| **מפעיל במפקדה** *(HQ Operator / סמל רוק"ק)* | • צפייה בתמונת השמיים החטיבתית.<br>• מדידת טווחים וגבהים. | • סינון ומיון בקשות תלויות.<br>• בדיקה גיאומטרית ראשונית.<br>• ללא סמכות אישור סופית. | • הזנת פרטי דיווח ראשוניים לאירוע.<br>• מעקב אחר ביצוע משימות.<br>• ללא סמכות הכרזת "פטיש אוויר". |
| **גורם מב"א / נציג אוגדה** *(Division / Maba Rep)* | • צפייה רחבה ואינטגרטיבית במספר חטיבות במקביל.<br>• סנכרון תמונת שמיים רחבה. | • פתרון קונפליקטים במרחבים משותפים (פרוזדורי מעבר בין חטיבות). | • פיקוח על אירועים רב-חטיבתיים.<br>• סמכות התערבות וביטול פוליגונים במקרה של סתירות צה"ליות. |

---

## 3. ארכיטקטורת הניווט (Navigation Proposal)
בשל הצורך בתגובה מהירה, הניווט יהיה **סרגל צד קבוע (Permanent Sidebar)** בעל מצב מורחב ומצומצם, המאפשר מעבר בלחיצה בודדת (Single-click) בין המסכים המרכזיים.

```
+------------------------------------------------------------------------------------+
| [Logo]  |  (MAP) תמנ"צ שמיים [התרעה: 2 מטרות חשודות]                                 |
|---------|--------------------------------------------------------------------------|
| (•) Map |  [ תור בקשות: 5 ]   [ אירועים פעילים: 1 ]   [ כלים באוויר: 12 ]          |
| (Q) Req |--------------------------------------------------------------------------|
| (F) Fly |                                                                          |
| (I) IFF |                               M A P                                      |
| (E) Evt |                          (GIS 2D/3D View)                                |
| (S) Spc |                                                                          |
|---------|                                                                          |
| [User]  |                                                                          |
+------------------------------------------------------------------------------------+
```

### קישורי הניווט הראשיים:
1. **תמנ"צ שמיים (Tactical Map):** מפת ה-COP המרכזית עם כלל השכבות.
2. **ניהול בקשות טיסה (Flight Requests Queue):** ממשק עבודה לטיפול בבקשות הממתינות.
3. **ניהול הטסה (Live Flights Tracker):** ניטור בזמן אמת של כלים באוויר ומדדי הפינג שלהם.
4. **לוח זיהוי עמית/טורף (IFF Resolver):** מרכז שליטה לפתרון מטרות לא מזוהות וסתירות מידע.
5. **מרכז אירועים (Operational Incidents):** ניהול ותיעוד אירועי "נמר" ו"פטיש אוויר".
6. **הגדרות וספקטרום (Settings & Spectrum Control):** ניהול תדרים ואזורי חסימות ל"א.

---

## 4. פירוט מסכי המערכת

### מסך 1: תמנ"צ (תמונת מצב שמיים) - Map & COP Screen

זהו מסך העבודה הראשי של קצין הרוק"ק והמפעילים במפקדה.

#### א. מבנה המסך:
* **מרכז המסך (80%):** מפת GIS אינטראקטיבית (דו-ממד/תלת-ממד).
* **סרגל ימני צף (Floating Panel):** רשימת מטרות פעילות (Tracks) מסוננת לפי סיווג (עמית, חשוד, עוין).
* **סרגל שמאלי צף:** בקרת שכבות מפה (פוליגונים, נתיבים, חסימות ספקטרום, סנסורים).
* **סרגל תחתון קבוע:** לוח בקרה והתרעות מהירות בזמן אמת (System Feed) עם כפתורי חירום להכרזת "נמר" מיידית.

#### ב. קומפוננטות UI:
* `MapWidget`: רכיב מפה מבוסס MapLibre GL. תומך בציור צורות (Polygons, Lines) ומציג סמלי כלים (Icons) זזים בזמן אמת.
* `LayerSelector`: כפתורי הדלקה/כיבוי של שכבות (רום, משואה, חסימות ל"א, סנסור מכ"ם, סנסור RF).
* `QuickActionPanel`: חלונית צפה הנפתחת בלחיצה על מטרה במפה, המציגה:
  * שם/מזהה מטרה, מהירות, גובה וסטטוס IFF.
  * כפתורים: **[הכרז נמר] (אדום)** | **[הפעל פטיש אוויר] (כתום)** | **[שנה סיווג לעמית] (ירוק)**.

#### ג. פעולות משתמש:
* הדלקה/כיבוי של שכבות מפה.
* סינון מטרות לפי גובה או מהירות.
* בחירת מטרה לצפייה בפרטים טלמטריים מלאים ובווקטור המהירות שלה.
* ציור ידני של פוליגון "אזור אסור לטיסה" (NFZ) זמני במקרה חירום.

---

### מסך 2: ניהול בקשות טיסה (Flight Requests Manager)

מיועד לטיפול בתוכניות טיסה המוגשות מהשטח (Android) או ממערכות משיקות.

#### א. מבנה המסך (מסך מפוצל - Split View):
* **צד ימין (40%):** תור בקשות טיסה (Pending Queue). הבקשות ממוינות לפי זמן המראה מתוכנן ורמת קונפליקט.
* **צד שמאל (60%):** כרטיסיית פירוט הבקשה שנבחרה הכוללת מפה ממוקדת המציגה את הנתיב המבוקש והקונפליקטים הגיאוגרפיים שנמצאו.

#### ב. קומפוננטות UI:
* `RequestCard`: כרטיס בקשה הכולל מזהה, שם מפעיל, סוג כלי, תדר עבודה, חלון זמן, ואינדיקטור סיווג צבעוני (ירוק/כתום/אדום).
* `ConflictDetailsWidget`: רכיב המציג את פלט מנוע החוקים הגיאומטרי:
  * פירוט חפיפות גובה/מרחק מול פוליגונים מאושרים אחרים.
  * פירוט חסימות ספקטרום (למשל: *"הנתיב עובר באזור חסימת ל''א פעילה בתדר 2.4GHz"*).
* `ResponseActionBar`: כפתורים לפעולה מיידית:
  * **[אישור בקשה]** (צבע ירוק)
  * **[דחיית בקשה]** (צבע אדום, פותח חלון הזנת סיבה)
  * **[החזרת הערות]** (צבע צהוב, מאפשר להציע נתיב או גובה חלופיים לשטח)

#### ג. מודל סיווג הבקשה (Traffic Light Classification):
* **ירוק (Green):**
  * *תנאי:* אפס חפיפות גיאומטריות, אפס אילוצי ספקטרום, גובה תקין.
  * *פעולה:* המערכת מאשרת אוטומטית או מציעה אישור בלחיצה אחת מהירה.
* **כתום (Orange):**
  * *תנאי:* חפיפה שולית בזמן או במרחב עם כלי אחר (למשל, הפרש גבהים קרוב לסף הבטיחות), או מעבר בשולי אזור חסימה עם סבירות נמוכה לשיבוש.
  * *פעולה:* מחייב בדיקה ידנית של המפעיל/קצין הרוק"ק. המערכת מציעה תיקון אוטומטי (למשל, הגבהת הנתיב ב-20 מטר).
* **אדום (Red):**
  * *תנאי:* חדירה מלאה ל-NFZ פעיל, עבודה בתדר חסום לחלוטין בגזרה, או חפיפה קשה עם נתיב מסוק מאויש (רום/משואה).
  * *פעולה:* חסימת אפשרות האישור האוטומטי. דורש אישור ידני מיוחד של קצין הרוק"ק תוך הזנת הנמקה מבצעית חריגה.

---

### מסך 3: ניהול הטסה (Live Flights Console)

מסך ניטור וניהול שוטף של כל הכלים הנמצאים כרגע באוויר או בשלבי המראה מיידיים.

```
+---------------------------------------------------------------------------------------------+
| כלי טיס פעילים באוויר (12)                                                                  |
+---------------------------------------------------------------------------------------------+
| [כלי: ינשוף-1] | [מפעיל: כוח אשד] | [גובה: 45מ']  | [פינג: תקין (1ש')] | [פוליגון: פעיל]      |
| [כלי: זיק-4]   | [מפעיל: צוות גדסר]| [גובה: 110מ'] | [פינג: תקין (2ש')] | [פוליגון: פעיל]      |
| [כלי: רחפן-9]  | [מפעיל: כוח גולן] | [גובה: 30מ']  | [פינג: אובדן קשר!] | [פוליגון: סגור ידנית]| (אדום מתפרץ)
+---------------------------------------------------------------------------------------------+
```

#### א. מבנה המסך:
* טבלת רשת (Grid Layout) של כרטיסי כלים פעילים.
* כל כרטיס משמש כ"מיני-לוח מחוונים" עבור כלי טיס בודד.

#### ב. קומפוננטות UI:
* `FlightLiveCard`: כרטיס המציג:
  * מזהה כלי + שם מפעיל בשטח.
  * נתוני טלמטרייה חיים: גובה נוכחי (לעומת הגובה המאושר בתוכנית), מהירות, אחוז סוללה.
  * `PingStatusIndicator`: נקודת חיווי צבעונית המייצגת את תדירות קבלת הפינגים מאפליקציית האול"ר (ירוק: < 2 שניות, צהוב: 2-10 שניות, אדום מהבהב: > 10 שניות - אובדן תקשורת).
  * `PolygonStatusToggle`: כפתור המאפשר לקצין הרוק"ק לכבות ידנית את הפוליגון המאושר במרחב האווירי (למשל, אם הכלי נחת מוקדם או אבד קשר והפוליגון מהווה מטרד במפה).

#### ג. מחזור סטטוס כלי טיס:
1. **ממתין (Pending):** הבקשה אושרה, אך הכלי טרם המריא או שידר טלמטרייה ראשונית.
2. **פעיל (Active):** הכלי באוויר, משדר טלמטריית מיקום תקינה ונמצא בתוך גבולות הנתיב המאושר שלו.
3. **הסתיים (Completed):** המפעיל דיווח על נחיתה או שחלון הזמן המאושר הסתיים והמיקום לא משודר יותר.
4. **אובדן תקשורת (Comms Loss):** לא התקבל פינג מהאול"ר במשך יותר מ-10 שניות. המערכת מקפיצה התרעה ויזואלית בכרטיס הכלי.
5. **חריג (Anomaly):** הכלי משדר מיקום, אך הוא נמצא בחריגה מוגדרת (למשל, חריגת גובה של מעל 15 מטרים מהתקרה המאושרת, או סטייה גיאוגרפית של מעל 50 מטרים מהפרוזדור המאושר).

---

### מסך 4: זיהוי עמית/טורף (IFF Resolver Dashboard)

ממשק ייעודי לפתרון סתירות וזיהוי מטרות לא מזוהות במרחב האווירי.

#### א. מבנה המסך:
* **חלק עליון (30%):** לוח מחוונים (KPIs) של מטרות לפי סיווג IFF נוכחי.
* **מרכז המסך (70%):** טבלת היתוך מידע המציגה הצלבות סנסור-תוכנית. משתמשים יכולים לראות מטרות מכ"ם/RF ולשייך אותן ידנית לכלים מאושרים במידת הצורך.

#### ב. קומפוננטות UI:
* `CertaintyIndicator`: סרגל התקדמות (Progress Bar) המציג את רמת הוודאות של המערכת לגבי הסיווג (למשל: *"85% ודאות - עמית מבוסס התאמת נתיב וטלמטריית אול''ר"*).
* `SensorDetailsWidget`: מציג אילו סנסורים קולטים כעת את המטרה (למשל: מכ"ם חטיבתי + מגן עליון + סנסור RF מקומי).
* `IFFActionButtons`: כפתורים מהירים לניהול הסיווג המבצעי:
  * **[הכרז כחול ודאי]** | **[הכרז אדום ודאי (עוין)]** | **[הפעל נוהל נמר]**.

#### ג. סטטוסים של מטרות שמיים במערכת:
* **כחול ודאי (Certain Friend):** יש התאמה מלאה: מזהה טרנספונדר פעיל + תוכנית טיסה מאושרת + סנסור פיזי מאמתים את אותו המיקום.
* **כחול חשוד (Suspicious Friend):** יש שידור טלמטרייה מאפליקציית אול"ר בשטח, אך אין תוכנית טיסה מאושרת רשומה במערכת לאזור זה.
* **כחול חריג (Anomalous Friend):** המפעיל משדר טלמטרייה ותוכנית טיסה קיימת, אך הכלי חורג משמעותית בגובה או בנתיב המאושרים (חשש לאובדן שליטה או טעות אנוש).
* **בלתי מזוהה (Unidentified):** מטרה נקלטת במכ"ם או בסנסור RF אך אין שום דיווח עצמי/טלמטרייה מהשטח באזור זה (ברירת המחדל לכל מטרה חדשה).
* **אדום חשוד (Suspicious Foe):** מטרה לא מזוהה שמציגה מאפייני טיסה חשודים (למשל: מהירות תנועה גבוהה, טיסה לכיוון כוחותינו, או תדר שידור המזוהה עם רחפני אויב).
* **אדום ודאי (Certain Foe):** אימות מלא של כלי עוין (למשל באמצעות זיהוי אופטי או אישור מודיעיני).
* **מידע סותר (Conflicting Data):** מצב בו יש סתירה בין סנסורים (למשל, סנסור א' מסווג את המטרה כרחפן עמית אך סנסור ב' מזהה חתימת RF עוינת). דורש הכרעה ידנית של קצין הרוק"ק.

---

### מסך 5: ניהול אירועים מבצעיים (Operational Incidents Console)

מיועד לניהול ומעקב אחר אירועים מורכבים במרחב האווירי (למשל: אירוע חדירה - "נמר", או הפעלת הגנה - "פטיש אוויר").

#### א. מבנה המסך:
* **שמאל (30%):** רשימת אירועים פעילים והיסטוריים.
* **מרכז (70%):** יומן כרונולוגי (Timeline) של האירוע שנבחר, המציג בצורה מובנית את כל הפעולות שבוצעו ומי ביצע אותן.

#### ב. קומפוננטות UI:
* `IncidentTimeline`: רכיב ויזואלי של ציר זמן המציג כרטיסי אירועים:
  * **[10:14:02]** - המטרה `sensor-radar-8812` נקלטה לראשונה (סיווג: בלתי מזוהה).
  * **[10:14:15]** - מפעיל מפקדה (יוסי) הזין דיווח ראשוני: "דיווח תצפית על רחפן שחור קטן".
  * **[10:15:00]** - קצין רוק"ק (דני) הכריז על נוהל **"נמר" (Tiger)**. סטטוס המטרה שונה לאדום ודאי.
  * **[10:15:05]** - הופצה התרעת Push אוטומטית לכל האול"רים בגזרה (רדיוס 5 ק"מ).
  * **[10:15:45]** - קצין רוק"ק הפעיל פקודת **"פטיש אוויר" (Hammer)**.
  * **[10:17:00]** - המטרה נעלמה מהסנסורים. קצין רוק"ק סגר את האירוע כנוטרל.
* `BroadcastTracker`: מציג רשימה של מכשירי הקצה בשטח (Android) ומאשר האם הם קיבלו ואישרו את קבלת ההתרעה (Read Receipts).

---

## 5. טבלאות מידע ומבנה נתונים (Data Tables)

להלן הגדרת מבנה הנתונים עבור קומפוננטות הטבלה בממשק ה-Web:

### 5.1. טבלת בקשות טיסה (`FlightRequestsTable`)
משמשת במסך ניהול בקשות הטיסה.

| שם השדה | סוג נתון | תיאור / ערכים |
| :--- | :--- | :--- |
| `id` | UUID | מזהה ייחודי |
| `timestamp` | DateTime | זמן הגשת הבקשה |
| `operator_name` | String | שם המפעיל המגיש |
| `unit` | String | יחידה צבאית של המפעיל (למשל: "גדוד 51") |
| `drone_model` | String | דגם הכלי (למשל: "Matrice 300") |
| `frequencies` | Array[Float] | תדרי שידור (למשל: `[2.4, 5.8]`) |
| `classification` | Enum | סיווג ראשוני: `GREEN`, `ORANGE`, `RED` |
| `conflict_summary` | String | תיאור קצר של הקונפליקטים (אם יש) |
| `time_window` | String | חלון זמן מבוקש (למשל: "14:00 - 14:30") |

### 5.2. טבלת מטרות שמיים חמות (`TracksTable`)
משמשת במסך תמנ"צ ובמסך IFF.

| שם השדה | סוג נתון | תיאור / ערכים |
| :--- | :--- | :--- |
| `track_id` | String | מזהה ייחודי של המטרה במרחב |
| `iff_status` | Enum | סיווג נוכחי: `FRIEND_CERTAIN`, `UNIDENTIFIED`, `FOE_CERTAIN`, etc. |
| `speed_kts` | Float | מהירות בקשרים / קמ"ש |
| `altitude_agl` | Float | גובה במטרים מעל פני השטח |
| `latitude` / `longitude` | Double | נקודות ציון גיאוגרפיות |
| `last_ping_seconds` | Integer | מספר השניות שחלפו מאז העדכון האחרון |
| `associated_request`| UUID (Null) | קישור לתוכנית טיסה מאושרת |

### 5.3. טבלת יומן פעולות אירוע (`IncidentLogsTable`)
משמשת בתוך ציר הזמן במסך ניהול האירועים.

| שם השדה | סוג נתון | תיאור / ערכים |
| :--- | :--- | :--- |
| `log_id` | UUID | מזהה ייחודי של הרשומה |
| `incident_id` | UUID | קישור לאירוע המבצעי |
| `timestamp` | DateTime | זמן ביצוע הפעולה |
| `action_type` | Enum | סוג הפעולה (למשל: `TIGER_DECLARED`, `HAMMER_DECLARED`, `BROADCAST_SENT`, `MANUAL_NOTE`) |
| `performed_by` | String | שם המשתמש שביצע את הפעולה (למשל: "דני - קצין רוק''ק") |
| `details` | String | פירוט טקסטואלי של הפעולה או הערה שהוזנה |

---

## 6. מצבי קיצון, ריק ושגיאות (Empty, Error & Edge States)

### 6.1. מצבי ריק (Empty States)
* **תור בקשות טיסה ריק:**
  * *ויזואליה:* איור של שמיים נקיים עם עננים קלים בגוון כחול-אפרפר עדין.
  * *טקסט:* "השמיים שקטים. אין בקשות טיסה ממתינות לאישור."
* **אין אירועים פעילים במרכז האירועים:**
  * *ויזואליה:* אייקון מגן ירוק ורגוע.
  * *טקסט:* "אין אירועים מבצעיים פעילים בגזרה כעת."

### 6.2. מצבי שגיאה (Error States)
* **אובדן תקשורת עם שרת הזרמת הטלמטרייה (WebSocket Connection Closed):**
  * *ויזואליה:* סרגל עליון בצבע אדום מהבהב עם חיווי סטטוס: **"מצב לא מקוון - מציג נתונים שמורים בלבד"**.
  * *טקסט עזר:* "המערכת מנסה להתחבר מחדש באופן אוטומטי. אנא ודא תקינות רשת המפקדה."
  * *מניעה מבצעית:* המפה תינעל לשינויים (Read-Only) כדי למנוע הזנת נתונים שגויה או אישור תוכניות טיסה על בסיס תמונה אווירית מיושנת.
* **כשל בקבלת נתוני סנסור חיצוני (רום / רש"י):**
  * *ויזואליה:* סימן אזהרה צהוב קטן ליד שכבת הסנסור הרלוונטית בסרגל השכבות.
  * *טקסט:* "שגיאה בקבלת נתוני רום. המידע המוצג עשוי להיות חלקי."

### 6.3. מקרי קצה מבצעיים (Operational Edge Cases)
* **מקרה קצה 1: דיווחים סותרים מסנסורים (Conflicting Sensors):**
  * *תיאור:* סנסור RF מזהה רחפן עמית (לפי תדר הוידאו), אך מכ"ם מזהה מהירות תנועה של 150 קמ"ש המתאימה לרחפן תוקף עוין.
  * *פתרון UX:* המערכת תסמן את המטרה בסטטוס **"מידע סותר"** בצבע כתום מהבהב עם אייקון של סימן שאלה וסימן קריאה משולבים. לחיצה על המטרה תציג את הנתונים הסותרים זה לצד זה עם כפתור בולט: **[הכרע ידנית: עמית / עוין]**.
* **מקרה קצה 2: הצפה של בקשות טיסה בו-זמנית (DDoS / Request Flooding):**
  * *תיאור:* במהלך מתקפה או תרגיל, עשרות מפעילים מגישים בקשות טיסה בשטח בו-זמנית, מה שעלול ליצור עומס על קצין הרוק"ק.
  * *פתרון UX:* מערכת ה-Web תפעיל מנגנון "קבץ בקשות דומות" (Batch Grouping). בקשות מאותו כוח או באותו תדר יקובצו תחת כרטיסייה אחת עם אפשרות ל-**[אישור קבוצתי]** לאחר בדיקת קונפליקטים משותפת.
* **מקרה קצה 3: אובדן פינג של כלי הנושא חימוש:**
  * *תיאור:* רחפן תוקף של כוחותינו באוויר מאבד קשר (Comms Loss) באזור רגיש.
  * *פתרון UX:* המערכת תקפיץ חלון אזהרה אדום קריטי המשתלט על 30% מהמסך: **"שים לב! אובדן קשר עם כלי חמוש (ינשוף-3). פוליגון הטיסה נשאר פעיל למניעת פגיעה עצמית. האם ברצונך לכבות את הפוליגון?"** עם אפשרויות בחירה ברורות: [השאר פוליגון פעיל] / [כבה פוליגון מייד].
